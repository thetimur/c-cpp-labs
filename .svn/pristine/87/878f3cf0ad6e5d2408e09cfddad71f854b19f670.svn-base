#ifndef HEADER_H_MERGESORT
#define HEADER_H_MERGESORT

#include "stdlib.h"

int mergesort(void *array, size_t elements, size_t element_size, int (*comparator)(const void*, const void*));

#endif // HEADER_H_MERGESORT